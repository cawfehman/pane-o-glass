/** @type {import('next').NextConfig} */
const nextConfig = {
    // ESLint and TypeScript are handled via root config in Next.js 15+
    typescript: { ignoreBuildErrors: true }
};
export default nextConfig;
